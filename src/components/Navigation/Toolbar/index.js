import React from 'react';
import Logo from '../../Logo';

const Toolbar = () => {
    return (
        <header className="Toolbar">
            <Logo/>
            <h1>Banca Transilvania</h1>
        </header>
    );
};

export default Toolbar;
