import React from 'react';

import btLogo from '../../assets/images/logo.png';

const Logo = () => {
    return (
        <div className="Logo">
            <img src={btLogo} alt="logo"/>
        </div>
    );
};

export default Logo;
